/* import { useState } from "react";

type FormProps = {
  newGuest: (param: string) => void;
};

function Header({ newGuest }: FormProps) {
  const [guest, setGuest] = useState<string[]>([]);

  return (
    <div>
      <p>{guest}</p>
    </div>
  )
}

export default Header;
 */